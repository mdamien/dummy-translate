# change the parameters here to choose the dictionnary you want to build

SOURCE = 'fra'
TARGET = 'nld'

TRANS_FILE = 'generated/{}_{}.csv'.format(SOURCE, TARGET)
DICT_FILE = 'generated/dict_{}_{}.json'.format(SOURCE, TARGET)